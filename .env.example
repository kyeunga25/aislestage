# Set with `wrangler secret put`, never commit production values.
OPENAI_API_KEY=
# Optional Worker variable. Keep deterministic unless assisted planning is deliberately enabled.
AGENT_MODE=deterministic
