{
  "name": "aislepack",
  "private": true,
  "version": "0.5.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "check": "npm run cf:types:check && tsc -b --pretty false",
    "cf:types": "wrangler types worker-env.d.ts --include-runtime false --env-interface WorkerEnv --strict-vars false --config wrangler.jsonc",
    "cf:types:check": "wrangler types worker-env.d.ts --include-runtime false --env-interface WorkerEnv --strict-vars false --check --config wrangler.jsonc",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "npm run cf:deploy",
    "cf:whoami": "wrangler whoami",
    "cf:migrate": "wrangler d1 migrations apply DB --remote --config wrangler.local.jsonc",
    "cf:invite": "node scripts/create-beta-invite.mjs",
    "cf:dry-run": "npm run build && wrangler deploy --dry-run --config wrangler.jsonc",
    "cf:deploy": "npm run build && wrangler deploy --keep-vars --config wrangler.local.jsonc",
    "cf:secrets": "wrangler secret list --config wrangler.local.jsonc"
  },
  "dependencies": {
    "agents": "^0.19.0",
    "jose": "^6.2.4",
    "lucide-react": "^1.27.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.8",
    "@cloudflare/workers-types": "^5.20260724.1",
    "@types/node": "^26.1.1",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "@vitejs/plugin-react": "^6.0.4",
    "typescript": "^7.0.2",
    "vite": "^8.1.5",
    "vitest": "^4.1.10",
    "wrangler": "^4.114.0"
  },
  "overrides": {
    "@hono/node-server": "^2.0.11"
  }
}
